import { StringTrimmerDirective } from './string-trimmer.directive';
// import {ElementRef} from "@angular/core";

describe('StringTrimmerDirective', () => {
  it('should create an instance', () => {

    // const directive = new StringTrimmerDirective(Element);
    // expect(directive).toBeTruthy();
  });
});
